<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.riging_test.template">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 외부 사진을 가져다 쓰기때문에 외부 저장소 접근권한이 필요함-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:name=".config.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Template">
        <activity
            android:name=".src.app_start.StartActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="etlmjycr7q" />
        <activity
            android:name=".src.test.Test"/>

        <activity
            android:name=".src.test.TestMainActivity"
            />


        <!-- sign_up-->
        <activity
            android:name=".src.sign_up.SignActivity"
            android:windowSoftInputMode="adjustResize"
            />

        <!-- main -->
        <activity
            android:name=".src.main.MainActivity"

            />

        <!-- search -->
        <activity
            android:name=".src.search.SearchActivity"
            />

        <!-- home_category -->
        <activity
            android:name=".src.home_category.HomeCategoryActivity"
            />

        <!-- alarm -->
        <activity
            android:name=".src.alarm.AlarmActivity"
            />
        <!-- product -->
        <activity
            android:name=".src.product.ProductActivity"
            />

        <!-- product_deal -->
        <activity
            android:name=".src.product_deal.ProductDealChatActivity"
            android:windowSoftInputMode="adjustResize"

            />


        <!-- posting -->
        <activity
            android:name=".src.posting.PostingActivity"
            android:windowSoftInputMode="adjustResize"
            />
        <!-- product_deal_time_setting-->
        <activity
            android:name=".src.product_deal_time_setting.TimeSettingActivity"
            />

        <!-- life_posting-->
        <activity
            android:name=".src.life_posting.LifePostingActivity"
            android:windowSoftInputMode="adjustResize"
            />

        <!-- life_posting_topics-->
        <activity
            android:name=".src.life_posting_topics.LifePostingTopicsActivity"
            />

        <!-- my_profile-->
        <activity
            android:name=".src.my_profile.MyProfileActivity"
            />

        <!-- my_carrot_sales_history-->
        <activity
            android:name=".src.my_carrot_sales_history.SalesHistoryActivity"
            />

        <!--my_location_setting-->
        <activity
            android:name=".src.my_location_setting.MyLocationSettingAcvitiy"
            />

        <!-- deal_finish-->
        <activity
            android:name=".src.deal_finish.DealFinish"
            />

        <!-- my_favorite-->
        <activity
            android:name=".src.my_favorite.MyFavoriteActivity"
            />

        <!-- chat_list -->
        <activity
            android:name=".src.chat_list.ChatList"
            />

        <activity
            android:name=".src.Test_Test"
            />
    </application>

</manifest>